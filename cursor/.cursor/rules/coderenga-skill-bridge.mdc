---
description: Route local coding-agent tasks to the coderenga Agent Skill. Applies to natural-language requests for repository inspection, review, debugging, implementation, refactoring, tests, and CodeRenga setup.
alwaysApply: true
---
# CodeRenga skill routing bridge

The procedural workflow lives in `.cursor/skills/coderenga/SKILL.md`.

For requests like `coderengaをインストールして`, `リポジトリを見て`, `レビューして`, `実装して`, `修正して`, `デバッグして`, `inspect this repo`, `review this change`, `fix this bug`, or `implement this`, use the `coderenga` Agent Skill proactively.

If CodeRenga is missing, the skill should install it from the latest `tksskt/CodeRenga` GitHub Release and initialize only when `coderenga.d` is missing. If no suitable release asset exists, stop and explain that a release asset is required.
